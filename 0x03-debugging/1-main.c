#include <stdo.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int 1;

	printf("infinite loop incoming :(\n");

	i = 0;
	/**
	 *while (i < 10)
	 *{
	 *       putchar(i);
	 *}
	 */
	printf("infinite loop avioded! \\0/\n");

	return (0);
}
